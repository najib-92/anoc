{% extends 'base.html.twig' %}

{% block title %}Techniciens{% endblock %}

{% block body %}
    <h1>Techniciens</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Technicienid</th>
                <td>{{ technicien.technicienid }}</td>
            </tr>
            <tr>
                <th>Personnelid</th>
                <td>{{ technicien.personnelid }}</td>
            </tr>
            <tr>
                <th>Diplome</th>
                <td>{{ technicien.diplome }}</td>
            </tr>
            <tr>
                <th>Specialite</th>
                <td>{{ technicien.specialite }}</td>
            </tr>
            <tr>
                <th>Dateoptdiplome</th>
                <td>{{ technicien.dateoptdiplome ? technicien.dateoptdiplome|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('techniciens_index') }}">back to list</a>

    <a href="{{ path('techniciens_edit', {'technicienid': technicien.technicienid}) }}">edit</a>

    {{ include('techniciens/_delete_form.html.twig') }}
{% endblock %}
