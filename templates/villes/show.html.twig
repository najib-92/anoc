{% extends 'base.html.twig' %}

{% block title %}Villes{% endblock %}

{% block body %}
    <h1>Villes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Villeid</th>
                <td>{{ ville.villeid }}</td>
            </tr>
            <tr>
                <th>Villefr</th>
                <td>{{ ville.villefr }}</td>
            </tr>
            <tr>
                <th>Villear</th>
                <td>{{ ville.villear }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('villes_index') }}">back to list</a>

    <a href="{{ path('villes_edit', {'villeid': ville.villeid}) }}">edit</a>

    {{ include('villes/_delete_form.html.twig') }}
{% endblock %}
